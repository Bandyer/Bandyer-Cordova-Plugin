/**
 * @typedef {Object} BandyerCallType
 * @property {string} AUDIO this is description.
 * @property {string} AUDIO_UPGRADABLE this is description.
 * @property {string} AUDIO_VIDEO this is description.
 */
enum BandyerCallType {
    AUDIO = "audio",
    AUDIO_UPGRADABLE = "audioUpgradable",
    AUDIO_VIDEO = "audioVideo"
}